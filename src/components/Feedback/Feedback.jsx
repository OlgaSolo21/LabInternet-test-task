import {Carousel} from "../Carousel/Carousel.jsx";

export function Feedback() {
    return (
        <section className="feedback">
            <h2 className="feedback__title heading">Отзывы</h2>
            <Carousel/>
        </section>
    )
}